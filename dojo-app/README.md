# Dojo App

Dojo Admin - Aikido Dojo Management System

## Quick Start

### Prerequisites

- Docker & Docker Compose
- Python 3.13+ (for local backend development)
- Node.js 22+ (for local frontend development)

### Running with Docker Compose

```bash
# Start all services
docker-compose up -d

# Access:
# - Frontend: http://localhost:3000
# - Backend API: http://localhost:8000
# - API Docs: http://localhost:8000/docs
```

### Backend Development

```bash
cd backend
poetry install
poetry run uvicorn app.main:app --reload
```

Run tests:
```bash
poetry run pytest              # Unit and integration tests
poetry run behave tests/bdd/   # Functional/BDD tests
```

### Frontend Development

```bash
cd frontend
npm install
npm run dev
```

Run tests:
```bash
npm test          # Unit tests
npm run test:e2e  # E2E tests with Cypress
```

## Project Structure

```
dojo-app/
├── backend/           # FastAPI + SQLAlchemy
│   ├── app/          # Application code
│   │   ├── api/      # API routes
│   │   ├── core/     # Config, security, database
│   │   ├── models/   # SQLAlchemy models
│   │   ├── schemas/  # Pydantic schemas
│   │   └── services/ # Business logic
│   ├── tests/        # Test suites
│   │   ├── unit/     # Unit tests
│   │   ├── integration/ # Integration tests
│   │   └── bdd/      # Functional/BDD tests (Behave)
│   └── alembic/      # Database migrations
├── frontend/         # React + Vite + TypeScript
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── services/
│   │   └── types/
│   └── cypress/      # E2E tests
└── docker-compose.yml # Local development
```

## Documentation

- [MVP Plan](../.omo/plans/mvp-plan.md)
- [Workflow State](../WORKFLOW_STATE.md)
- [API Docs](http://localhost:8000/docs) (auto-generated by FastAPI)

## Tech Stack

- **Backend:** Python 3.13, FastAPI, SQLAlchemy 2.0, Alembic, MySQL 8.0
- **Frontend:** React 19, TypeScript, Vite, Tailwind CSS, TanStack Query
- **Testing:** pytest (unit/integration), Behave (BDD), Cypress (E2E)
- **Infra:** OCI, OKS, Terraform, Kubernetes
