FROM node:22-alpine

WORKDIR /app

# Install system dependencies for Cypress E2E tests (Xvfb for headless browser)
RUN apk add --no-cache xvfb

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy application code
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host"]
